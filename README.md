This project is a simple Student Information Management System developed using Java. It demonstrates the use of constructors (both default and parameterized) to initialize and manage student data efficiently.

The system allows the creation of student objects with details such as name, roll number, age. Constructors are used to ensure that each student object is initialized with appropriate values at the time of creation, improving code readability and reducing errors.
